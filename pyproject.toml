[project]
name = "geometric-transportability"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "scipy",
    "scikit-learn",
    "pandas",
    "networkx",
    "matplotlib",
    "requests",
    "tqdm",
    "xgboost",
    "pyyaml",
    "pytest",
]
